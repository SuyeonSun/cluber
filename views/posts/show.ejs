<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
</head>
<body>
    <h1>상세 페이지 입니다.</h1>
    작성자 <%= rows.username %> <br>
    제목 <%= rows.title %> <br>
    내용 <%= rows.body %> <br>

    <% if ((logged) & (user.username == rows.username)) { %>
        <a href='/posts/edit/<%= rows.id %>'>수정하기</a>
        <a href='/posts/delete/<%= rows.id %>'>삭제하기</a>
    <% } %>
</body>
</html>

    