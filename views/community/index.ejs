<!DOCTYPE html>
<head>
    <%- include('../partials/head') %>
</head>
<body>
    <%- include('../partials/nav') %>
    <div class="container mb-3">

    <h1>커뮤니티 페이지</h1>
    <a href="/community/new">커뮤니티 글 작성하러 가기</a>
    <% if(rows == null || rows.length == 0){ %>
        <tr>
          <td> 아직 작성된 글이 없습니다. </td>
        </tr>
    <% } %>

    <% rows.forEach(element => { %>
        <%= element.title %>
        <%= element.username %> <!--작성자(username)-->
        <a href='/community/show/<%= element.id %>'>상세보기</a>
    <% }); %> 
</div>
</body>
</html>